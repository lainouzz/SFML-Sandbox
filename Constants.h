#pragma once

inline const int WINDOW_WIDTH = 800;
inline const int WINDOW_HEIGHT = 600;
inline const int CELL_SIZE = 4;

inline const int GRID_WIDTH = WINDOW_WIDTH / CELL_SIZE;
inline const int GRID_HEIGHT = WINDOW_HEIGHT / CELL_SIZE;


extern int brushRadius;
extern int grainCount;